version: '3.8'

services:
  web:
    build: .
    volumes:
      - /var/www/heyvoca_back/${SOCKET_NAME}.sock:/app/${SOCKET_NAME}.sock  # 유닉스 소켓 파일 연결
      - .:/app
    env_file:
      - .env.prod
    restart: always
